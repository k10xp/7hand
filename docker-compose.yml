services:
  # Main nginx reverse proxy for EC2 deployment
  # Routes traffic to frontend and backend services
  sevenhand-proxy:
    build: ./nginx-proxy
    container_name: sevenhand-proxy
    ports:
      - '8000:8000'
    depends_on:
      - frontend
      - backend
    networks:
      - app-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      target: prod
    depends_on:
      - backend
    networks:
      - app-network

  frontend-dev:
    build:
      context: ./frontend
      target: dev
      dockerfile: Dockerfile
    ports:
      - '4200:4200'
    depends_on:
      - backend
      - db
    environment:
      CI: 'false'
      CHOKIDAR_USEPOLLING: 'true'
    volumes:
      - ./frontend:/workspace:cached
      - frontend-node_modules:/workspace/node_modules
    command: ['npm', 'start', '--', '--host', '0.0.0.0', '--poll', '2000']
    networks:
      - app-network

  backend:
    build: ./backend
    environment:
      NODE_ENV: production
      DB_HOST: db
      DB_PORT: 5432
      DB_USER: ${DB_USER:-card-game}
      DB_PASSWORD: ${DB_PASSWORD:-changeme}
      DB_NAME: ${DB_NAME:-card-game-db}
      DATABASE_URL: postgres://${DB_USER:-card-game}:${DB_PASSWORD:-changeme}@db:5432/${DB_NAME:-card-game-db}
    depends_on:
      - db
    networks:
      - app-network

  db:
    image: postgres:16
    restart: always
    environment:
      POSTGRES_USER: ${DB_USER:-card-game}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
      POSTGRES_DB: ${DB_NAME:-card-game-db}
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - app-network

  # Selenium Grid Hub
  selenium-hub:
    image: selenium/hub:4.15.0
    container_name: selenium-hub
    ports:
      - '4444:4444'

  # Chrome Node
  selenium-chrome:
    image: selenium/node-chrome:4.15.0
    container_name: selenium-chrome
    shm_size: 2gb
    depends_on:
      - selenium-hub
    environment:
      SE_EVENT_BUS_HOST: selenium-hub
      SE_EVENT_BUS_PUBLISH_PORT: 4442
      SE_EVENT_BUS_SUBSCRIBE_PORT: 4443

  # Firefox Node
  selenium-firefox:
    image: selenium/node-firefox:4.15.0
    container_name: selenium-firefox
    shm_size: 2gb
    depends_on:
      - selenium-hub
    environment:
      SE_EVENT_BUS_HOST: selenium-hub
      SE_EVENT_BUS_PUBLISH_PORT: 4442
      SE_EVENT_BUS_SUBSCRIBE_PORT: 4443

  # Selenium Tests
  selenium-tests:
    build: ./selenium
    container_name: selenium-tests
    depends_on:
      - selenium-hub
      - selenium-chrome
      - selenium-firefox
    environment:
      SELENIUM_REMOTE_URL: http://selenium-hub:4444/wd/hub
    ports:
      - '8080:8080'
    command: ['sh', '-c', 'sleep 30 && npm run test:demo']

volumes:
  db-data:
  frontend-node_modules:

networks:
  app-network:
    driver: bridge
